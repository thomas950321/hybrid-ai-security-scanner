{
  "name": "@hybrid/analyst-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@hybrid/contracts": "workspace:*",
    "@hybrid/retrieval": "workspace:*"
  }
}

